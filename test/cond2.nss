void main () {
    int x = 0;
    int y;
    int z;
    if (x > 0) {
        y = 1;
    } else {
        int z = 0;
        object a;
        vector v = [0.0, 0.0, 0.0];
    }
    PrintInteger(y);
}