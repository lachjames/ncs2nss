// int A = 0;
// int B = 1;

void main() {
    int a = 0;
    int b = 1;
    int c = a + b;

    // Override a variable
    //a = a + b + c;

    // Create a variable which is never used
    //int d = 0;

    // Create a variable with no assigned value, and then assign it
    //int e;
    //e = a + b;

    // Create a variable with no assigned value, and don't assign it
    //int f;
}